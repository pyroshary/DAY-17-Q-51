//making a funtion to calculte rectangle area
function majorarea( width: number, height:number): number{return width*height};
//making arrow function to refactored data
let area =( width: number, height:number): number => width*height;

console.log(area(5,10));